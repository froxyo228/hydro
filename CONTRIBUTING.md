# 🤝 Руководство по участию в разработке HydroSim 2.0

Добро пожаловать в проект HydroSim 2.0! Мы рады вашему участию в разработке симулятора гидроэлектростанций.

## 📋 Стиль коммит-сообщений

Мы используем формат [Conventional Commits](https://www.conventionalcommits.org/) для единообразия истории изменений:

### Типы коммитов:
- `feat:` - новая функциональность
- `fix:` - исправление ошибок
- `refactor:` - рефакторинг кода без изменения функциональности
- `docs:` - изменения в документации
- `style:` - форматирование, отступы (без изменения логики)
- `test:` - добавление или изменение тестов
- `chore:` - обновление зависимостей, настройки сборки

### Примеры:
```
feat: добавлен контроль прочности плотины
fix: починена логика отключения кнопки "Построить"
refactor: переорганизованы файлы по папкам core/ и systems/
docs: обновлено руководство по установке
```

## 🏗️ Процесс внесения изменений

### 1. Создание веток
- `feature/название-функции` - для новых возможностей
- `bugfix/описание-бага` - для исправления ошибок
- `refactor/область-рефакторинга` - для рефакторинга
- `docs/тема-документации` - для документации

### 2. Разработка
- Делайте небольшие, осмысленные коммиты
- Каждый коммит должен представлять законченное изменение
- Тестируйте изменения перед коммитом

### 3. Pull Request
- Создавайте PR с описательным названием
- Добавляйте описание изменений
- Ссылайтесь на связанные задачи (если есть)

## 🎨 Стиль кода

### GDScript
- **Отступы**: 4 пробела (не табы)
- **Именование переменных**: `snake_case`
- **Именование функций**: `snake_case`
- **Именование классов**: `PascalCase`
- **Константы**: `UPPER_SNAKE_CASE`

### Комментарии
- Комментарии к коду на русском языке
- Документация к публичным методам обязательна
- Используйте `##` для документации функций

### Пример:
```gdscript
# ========== GameManager.gd ==========
# Основной менеджер игры - управляет состояниями и ресурсами
extends Node

const MAX_MONEY = 1000000
var current_money: int = 100000

## Потратить деньги на покупку
## @param amount - сумма к списанию
## @return bool - успешно ли выполнена операция
func spend_money(amount: int) -> bool:
    if current_money >= amount:
        current_money -= amount
        return true
    return false
```

## 🏷️ Логирование

Используйте единую систему тегов для отладочных сообщений:

- `[BUILD]` - строительство плотин
- `[SURVEY]` - георазведка
- `[STATE]` - изменения состояния игры
- `[RESOURCE]` - экономика и ресурсы
- `[SYSTEM]` - инициализация систем
- `[UI]` - интерфейс пользователя
- `[SAVE]` - сохранение/загрузка

```gdscript
print("[BUILD] Плотина построена в зоне %s" % zone_id)
print("[SURVEY] Георазведка завершена")
```

## 📁 Структура проекта

```
hydrosim2.0/
├── scripts/
│   ├── core/           # Основные менеджеры (GameManager, ConfigManager)
│   ├── systems/        # Игровые системы (EconomicSystem, RiverSystem)
│   ├── ui/            # Интерфейс пользователя
│   ├── build/         # Система строительства
│   ├── world/         # Мировые данные (GeoData)
│   └── debug/         # Отладочные инструменты
├── scenes/
│   ├── ui/            # Сцены интерфейса
│   ├── build/         # Сцены строительства
│   └── ...
└── sprites/           # Графические ресурсы
```

## 🧪 Тестирование

### Перед коммитом:
1. Запустите игру и проверьте основной функционал
2. Убедитесь, что нет критических ошибок в консоли
3. Проверьте, что новые функции работают как ожидается

### Тестовые сценарии:
- Запуск игры и переход в основное меню
- Начало новой игры
- Георазведка и строительство плотины
- Сохранение и загрузка игры
- Меню паузы (ESC)

## 🐛 Сообщения об ошибках

При создании issue включите:
- **Описание проблемы**: что происходит и что ожидалось
- **Шаги воспроизведения**: как повторить ошибку
- **Версия Godot**: какую версию используете
- **Скриншоты/логи**: если применимо

## 📚 Документация

- Обновляйте `CHANGELOG.md` при значительных изменениях
- Документируйте новые публичные методы
- Обновляйте README.md при изменении установки/запуска

## 🎯 Приоритеты разработки

1. **Стабильность** - исправление ошибок имеет приоритет
2. **Производительность** - оптимизация критических участков
3. **Новые функции** - расширение геймплея
4. **Улучшение UX** - удобство интерфейса

## 💬 Связь

- Обсуждения в Issues
- Вопросы по коду в Pull Requests
- Предложения по улучшениям в Discussions

---

**Спасибо за участие в развитии HydroSim 2.0!** 🚀

Ваш вклад помогает создать лучший симулятор управления гидроэлектростанциями.
